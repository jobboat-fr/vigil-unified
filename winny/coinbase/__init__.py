"""Coinbase CDP integrations grouped by product.

Currently disabled-but-scaffolded modules live here so the namespace
is reserved and the feature contract is explicit. Each module gates
its calls behind the corresponding WW_FEATURE_* flag.
"""
